@model page.Models.Destination
@using Microsoft.AspNetCore.Identity
@inject UserManager<page.Models.ApplicationUser> UserManager
@{
    ViewData["Title"] = Model.Name;
    Layout = "_Layout";
    var bgImageUrl = !string.IsNullOrEmpty(Model.ImageUrl) ? Model.ImageUrl : "https://images.unsplash.com/photo-1559592413-7cec4d0cae2b?auto=format&fit=crop&q=80";
    var isSaved = ViewBag.IsSaved as bool? ?? false;
    var isVisited = ViewBag.IsVisited as bool? ?? false;
    var sentimentTags = ViewBag.SentimentTags as Dictionary<string, int> ?? new Dictionary<string, int>();
    var heritageTimeline = ViewBag.HeritageTimeline as List<page.Controllers.HeritageTimelineEntry> ?? new List<page.Controllers.HeritageTimelineEntry>();
    var recentStories = ViewBag.RecentStories as List<page.Models.TravelStory> ?? new List<page.Models.TravelStory>();
}

@functions {
    public string FormatUserName(string? fullName, string? userName)
    {
        if (!string.IsNullOrEmpty(fullName)) return fullName;
        if (string.IsNullOrEmpty(userName)) return "Ẩn danh";
        
        if (userName.Contains("@"))
        {
            var parts = userName.Split('@');
            if (parts[0].Length <= 2) return userName.Substring(0, 1) + "***@" + parts[1];
            return parts[0].Substring(0, 2) + "***@" + parts[1];
        }
        return userName;
    }

    public string GetRelativeTime(DateTime dateTime)
    {
        var timeSpan = DateTime.UtcNow - dateTime;
        if (timeSpan <= TimeSpan.FromSeconds(60)) return "Vừa xong";
        if (timeSpan <= TimeSpan.FromMinutes(60)) return $"{timeSpan.Minutes} phút trước";
        if (timeSpan <= TimeSpan.FromHours(24)) return $"{timeSpan.Hours} giờ trước";
        if (timeSpan <= TimeSpan.FromDays(30)) return $"{timeSpan.Days} ngày trước";
        if (timeSpan <= TimeSpan.FromDays(365)) return $"{timeSpan.Days / 30} tháng trước";
        return $"{timeSpan.Days / 365} năm trước";
    }
}

<div class="section" style="background-color: transparent; padding-top: calc(var(--nav-h) + var(--topbar-h) + 2rem); padding-bottom: 5rem; max-width: 1200px; margin: 0 auto; padding-left: 15px; padding-right: 15px;">
    <div class="container-fluid p-0" style="max-width: var(--max-w);">
        <div class="mb-4 reveal">
            <a asp-action="Index" class="btn rounded-pill fw-medium" style="background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2); color: white; transition: all 0.3s;"><i class="bi bi-arrow-left me-1"></i> Quay lại danh sách</a>
        </div>
        
        <div class="mb-5 reveal text-white">
            <div class="badge px-3 py-2 rounded-pill mb-3" style="background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2);"><i class="bi bi-geo-alt-fill text-danger me-1"></i> @Model.Location</div>
            <h1 class="fw-bold mb-3" style="font-size: clamp(2rem, 4vw, 3.5rem);">@Model.Name</h1>
            <div class="d-flex flex-wrap gap-3">
                <span class="badge bg-danger px-4 py-2 rounded-pill fs-6 fw-medium shadow-sm"><i class="bi bi-tag-fill me-1"></i> @Model.Type</span>
                <span class="badge bg-warning text-dark px-4 py-2 rounded-pill fs-6 fw-bold shadow-sm"><i class="bi bi-star-fill me-1"></i> @Model.AverageRating.ToString("0.0")</span>
            </div>
        </div>
        
        <div class="row g-4">
            <!-- Main Content -->
            <div class="col-lg-8 reveal-left">
                <div class="card border-0 mb-4 shadow-sm" style="border-radius: var(--radius-lg); overflow: hidden; background: rgba(20, 20, 25, 0.8); border: 1px solid rgba(255,255,255,0.05) !important;">
                    <div class="row g-0">
                        <div class="col-md-5">
                            <img src="@bgImageUrl" alt="@Model.Name" class="img-fluid h-100" style="object-fit: cover; width: 100%; min-height: 300px; border-right: 1px solid rgba(255,255,255,0.05);" />
                        </div>
                        <div class="col-md-7">
                            <div class="card-body p-4 p-md-5">
                                <h3 class="fw-bold mb-4 text-white" style="font-size: 1.5rem;"><i class="bi bi-info-circle text-danger me-2"></i>Thông tin chi tiết</h3>
                                
                                <div class="d-flex flex-wrap gap-4 mb-4 pb-4" style="border-bottom: 1px solid rgba(255,255,255,0.1);">
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle d-flex align-items-center justify-content-center text-danger me-3 shadow-sm" style="width: 54px; height: 54px; background: rgba(255,255,255,0.05);">
                                            <i class="bi bi-cash-stack fs-4"></i>
                                        </div>
                                        <div>
                                            <div class="small fw-bold text-uppercase letter-spacing-1" style="color: #aaa;">Chi phí dự kiến</div>
                                            <div class="fw-bold fs-5" style="color: var(--gold);">
                                                @if (Model.EstimatedCost > 0)
                                                {
                                                    @string.Format("{0:N0} VNĐ", Model.EstimatedCost)
                                                }
                                                else
                                                {
                                                    <span class="text-success"><i class="bi bi-check-circle-fill me-1"></i>Miễn phí</span>
                                                }
                                            </div>
                                        </div>
                                    </div>
                                    <div class="d-flex align-items-center">
                                        <div class="rounded-circle d-flex align-items-center justify-content-center text-danger me-3 shadow-sm" style="width: 54px; height: 54px; background: rgba(255,255,255,0.05);">
                                            <i class="bi bi-people fs-4"></i>
                                        </div>
                                        <div>
                                            <div class="small fw-bold text-uppercase letter-spacing-1" style="color: #aaa;">Đánh giá</div>
                                            <div class="fw-bold text-white fs-5">@(Model.Reviews?.Count ?? 0) người</div>
                                        </div>
                                    </div>
                                </div>

                                <div class="description-content" style="line-height: 1.85; color: #ccc; font-size: 1.05rem; white-space: pre-wrap;">@Model.Description</div>

                                @if (sentimentTags.Any())
                                {
                                    <div class="mt-4 pt-4" style="border-top: 1px solid rgba(255,255,255,0.1);">
                                        <h5 class="text-white fw-bold mb-3"><i class="bi bi-tags text-warning me-2"></i>Điểm nổi bật từ đánh giá</h5>
                                        <div class="d-flex flex-wrap gap-2">
                                            @foreach (var tag in sentimentTags)
                                            {
                                                <span class="badge rounded-pill px-3 py-2" style="background: rgba(212,175,55,0.15); color: var(--gold); border: 1px solid rgba(212,175,55,0.3);">
                                                    @tag.Key (@tag.Value)
                                                </span>
                                            }
                                        </div>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </div>

                @if (Model.Images != null && Model.Images.Any())
                {
                    <div class="card border-0 mb-4 shadow-sm" style="border-radius: var(--radius-lg); overflow: hidden; background: rgba(20, 20, 25, 0.8); border: 1px solid rgba(255,255,255,0.05) !important;">
                        <div class="card-body p-4">
                            <h3 class="fw-bold mb-3 text-white"><i class="bi bi-images text-danger me-2"></i>Thư viện ảnh</h3>
                            <div class="row g-2">
                                @foreach (var img in Model.Images.OrderBy(i => i.SortOrder))
                                {
                                    <div class="col-4 col-md-3">
                                        <a href="@img.Url" target="_blank">
                                            <img src="@img.Url" alt="@(img.Caption ?? Model.Name)" class="img-fluid rounded-3" style="height: 120px; width: 100%; object-fit: cover; cursor: pointer; border: 1px solid rgba(255,255,255,0.1);">
                                        </a>
                                        @if (!string.IsNullOrEmpty(img.Caption))
                                        {
                                            <div class="small text-muted mt-1 text-center">@img.Caption</div>
                                        }
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                }



                @if (heritageTimeline.Any())
                {
                    <div class="card border-0 mb-4 shadow-sm" style="border-radius: var(--radius-lg); background: rgba(20, 20, 25, 0.8); border: 1px solid rgba(255,255,255,0.05) !important;">
                        <div class="card-body p-4 p-md-5">
                            <h3 class="fw-bold mb-4 text-white"><i class="bi bi-clock-history text-danger me-2"></i>Di Sản Qua Các Thời Kỳ</h3>
                            <div class="heritage-timeline">
                                @foreach (var entry in heritageTimeline)
                                {
                                    <div class="timeline-item mb-4 ps-4 border-start border-warning" style="border-width: 2px !important;">
                                        <div class="small text-warning fw-bold">@entry.Year</div>
                                        <div class="text-white fw-bold">@entry.Title</div>
                                        <div class="text-muted small">@entry.Description</div>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                }

                @if (recentStories.Any())
                {
                    <div class="card border-0 mb-4" style="border-radius: var(--radius-lg); background: rgba(20, 20, 25, 0.8); border: 1px solid rgba(255,255,255,0.05) !important;">
                        <div class="card-body p-4">
                            <h4 class="text-white fw-bold mb-3"><i class="bi bi-journal-text text-danger me-2"></i>Câu chuyện từ du khách</h4>
                            @foreach (var story in recentStories)
                            {
                                <div class="mb-3 pb-3 border-bottom" style="border-color: rgba(255,255,255,0.08) !important;">
                                    <div class="fw-bold text-white">@story.Title</div>
                                    <div class="small text-muted mb-1">@(story.User?.FullName ?? story.User?.UserName) · @story.CreatedAt.ToString("dd/MM/yyyy")</div>
                                    <p class="text-muted small mb-0">@(story.Content.Length > 150 ? story.Content.Substring(0, 150) + "..." : story.Content)</p>
                                </div>
                            }
                            <a asp-controller="Stories" asp-action="Index" class="btn btn-sm btn-outline-light rounded-pill">Xem tất cả</a>
                        </div>
                    </div>
                }

            <!-- Sidebar -->
            <div class="col-lg-4 reveal-right">
                <!-- Action Card -->
                <div class="card border-0 mb-4 shadow-md" style="border-radius: var(--radius-lg); background: rgba(20, 20, 25, 0.8); border: 1px solid rgba(255,255,255,0.05) !important;">
                    <div class="card-body p-4 text-center">
                        <div class="display-3 fw-bold text-warning mb-1" style="text-shadow: 0 2px 10px rgba(249, 168, 37, 0.2);">@Model.AverageRating.ToString("0.0")</div>
                        <div class="text-warning mb-2 fs-5">
                            @for(int i=1; i<=5; i++) {
                                if (i <= Math.Round(Model.AverageRating)) { <i class="bi bi-star-fill"></i> }
                                else { <i class="bi bi-star"></i> }
                            }
                        </div>
                        <p class="small mb-4" style="color: #888;">Dựa trên @(Model.Reviews?.Count ?? 0) đánh giá từ người dùng</p>
                        
                        @if (User.Identity?.IsAuthenticated == true)
                        {
                            @if (isSaved)
                            {
                                <form asp-action="UnsaveDestination" method="post" class="mb-2">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="destinationId" value="@Model.Id" />
                                    <button type="submit" class="btn w-100 rounded-pill py-2 fw-bold" style="background: var(--red); color: white; border: none;">
                                        <i class="bi bi-heart-fill me-1"></i> Đã lưu yêu thích
                                    </button>
                                </form>
                            }
                            else
                            {
                                <form asp-action="SaveDestination" method="post" class="mb-2">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="destinationId" value="@Model.Id" />
                                    <button type="submit" class="btn btn-outline-danger w-100 rounded-pill py-2 fw-bold" style="border-width: 2px;">
                                        <i class="bi bi-heart me-1"></i> Lưu vào yêu thích
                                    </button>
                                </form>
                            }

                            @if (!isVisited)
                            {
                                <form asp-action="MarkVisited" method="post" class="mb-2">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="destinationId" value="@Model.Id" />
                                    <button type="submit" class="btn btn-outline-warning w-100 rounded-pill py-2 fw-bold">
                                        <i class="bi bi-patch-check me-1"></i> Đánh dấu đã đến
                                    </button>
                                </form>
                            }
                            else
                            {
                                <div class="alert py-2 mb-2 rounded-pill text-center small" style="background: rgba(249,168,37,0.15); color: var(--gold); border: 1px solid rgba(249,168,37,0.3);">
                                    <i class="bi bi-patch-check-fill me-1"></i> Đã đến
                                </div>
                            }

                            <a asp-controller="Passport" asp-action="Index" class="btn btn-outline-light w-100 rounded-pill py-2 mb-2">
                                <i class="bi bi-passport me-1"></i> Xem hộ chiếu
                            </a>
                        }
                        else
                        {
                            <a href="/Identity/Account/Login" class="btn w-100 rounded-pill py-2 fw-bold shadow-sm" style="background: linear-gradient(135deg, var(--red), #8E1B1B); color: white; border: none;">
                                <i class="bi bi-box-arrow-in-right me-1"></i> Đăng nhập để lưu
                            </a>
                        }
                    </div>
                </div>

                @if (User.IsInRole("Admin"))
                {
                    <div class="card border-0 mb-4" style="border-radius: var(--radius-lg); background: rgba(20, 20, 25, 0.8); border: 1px solid rgba(255,255,255,0.05) !important;">
                        <div class="card-body p-4">
                            <h5 class="text-white fw-bold mb-3"><i class="bi bi-shield-lock text-warning me-2"></i>Quản trị</h5>
                            <div class="d-flex flex-wrap gap-2">
                                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-light rounded-pill"><i class="bi bi-pencil me-1"></i>Sửa</a>

                                <form asp-action="Delete" method="post" onsubmit="return confirm('Bạn có chắc chắn muốn xóa điểm đến này?');" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@Model.Id" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger rounded-pill"><i class="bi bi-trash me-1"></i>Xóa</button>
                                </form>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>

        <!-- Reviews Section (Full Width) -->
        <div class="card border-0 shadow-sm mt-4 reveal" style="border-radius: var(--radius-lg); background: rgba(20, 20, 25, 0.8); border: 1px solid rgba(255,255,255,0.05) !important;">
            <div class="card-header border-bottom-0 pt-4 pb-0 px-4" style="background: transparent;">
                <h4 class="fw-bold mb-0 text-white"><i class="bi bi-chat-square-text text-danger me-2"></i>Cộng đồng đánh giá</h4>
            </div>
            <div class="card-body p-4">
                @if (User.Identity?.IsAuthenticated == true)
                {
                    var currentUserId = UserManager.GetUserId(User);
                    var hasReviewed = Model.Reviews != null && Model.Reviews.Any(r => r.UserId == currentUserId);

                    if (hasReviewed)
                    {
                        <div class="alert border rounded-4 text-center mb-4 py-3" style="background: rgba(0,0,0,0.3); border-color: rgba(255,255,255,0.05) !important; color: #ccc;">
                            Bạn đã đánh giá địa điểm này. Cảm ơn sự đóng góp của bạn!
                        </div>
                    }
                    else
                    {
                        <form id="reviewForm" asp-action="AddReview" method="post" class="mb-4 p-4 rounded-4 border" style="background: rgba(0,0,0,0.3); border-color: rgba(255,255,255,0.05) !important;">
                            @Html.AntiForgeryToken()
                            <input type="hidden" name="destinationId" value="@Model.Id" />
                            <div class="mb-3">
                                <label class="form-label fw-bold small text-uppercase letter-spacing-1" style="color: #aaa;">Chấm điểm của bạn</label>
                                <div class="rating-input d-flex flex-row-reverse justify-content-end gap-1">
                                    <input type="radio" name="rating" value="5" id="star5" required><label for="star5" class="bi bi-star-fill text-warning fs-4 cursor-pointer"></label>
                                    <input type="radio" name="rating" value="4" id="star4"><label for="star4" class="bi bi-star-fill text-warning fs-4 cursor-pointer"></label>
                                    <input type="radio" name="rating" value="3" id="star3"><label for="star3" class="bi bi-star-fill text-warning fs-4 cursor-pointer"></label>
                                    <input type="radio" name="rating" value="2" id="star2"><label for="star2" class="bi bi-star-fill text-warning fs-4 cursor-pointer"></label>
                                    <input type="radio" name="rating" value="1" id="star1"><label for="star1" class="bi bi-star-fill text-warning fs-4 cursor-pointer"></label>
                                </div>
                                <style>
                                    .rating-input input { display: none; }
                                    .rating-input label { color: rgba(255,255,255,0.2) !important; transition: color 0.2s, transform 0.2s; cursor: pointer; }
                                    .rating-input label:hover,
                                    .rating-input label:hover ~ label,
                                    .rating-input input:checked ~ label { color: #ffc107 !important; transform: scale(1.1); }
                                </style>
                            </div>
                            <div class="mb-4 position-relative">
                                <textarea id="reviewComment" name="comment" class="form-control shadow-sm custom-dark-input" rows="3" placeholder="Gõ vài từ (ví dụ: đẹp, đồ ăn ngon)..." required style="border: 1px solid rgba(255,255,255,0.2) !important; border-radius: var(--radius); resize: none; font-size: 0.95rem; background: rgba(255,255,255,0.05); color: white; padding-bottom: 2.5rem;"></textarea>
                                <button type="button" id="btnAIEnhance" class="btn btn-sm position-absolute bottom-0 end-0 m-2 rounded-pill" style="background: rgba(255,255,255,0.1); color: #fff; border: 1px solid rgba(255,255,255,0.2); transition: all 0.3s; z-index: 10;" onmouseover="this.style.background='rgba(255,255,255,0.2)'" onmouseout="this.style.background='rgba(255,255,255,0.1)'">
                                    <i class="bi bi-magic text-warning me-1"></i> Trợ lý AI (Viết dài)
                                </button>
                            </div>
                            <button type="submit" class="btn rounded-pill fw-bold" style="background: var(--gold); color: #000; padding: 0.8rem 2.5rem; transition: all 0.3s;" onmouseover="this.style.filter='brightness(1.15)'" onmouseout="this.style.filter='brightness(1)'">Gửi đánh giá</button>
                        </form>
                    }
                }
                else
                {
                    <div class="alert border rounded-4 text-center mb-4 py-3" style="background: rgba(0,0,0,0.3); border-color: rgba(255,255,255,0.05) !important; color: #ccc;">
                        Vui lòng <a href="/Identity/Account/Login" class="fw-bold text-danger text-decoration-none">đăng nhập</a> để chia sẻ trải nghiệm.
                    </div>
                }

                <div class="review-list custom-scrollbar pe-2" style="max-height: 500px; overflow-y: auto;">
                    @if (Model.Reviews != null && Model.Reviews.Any())
                    {
                        foreach (var review in Model.Reviews.OrderByDescending(r => r.CreatedAt))
                        {
                            <div class="mb-4 pb-3 border-bottom" style="border-color: rgba(255,255,255,0.05) !important;">
                                <div class="d-flex align-items-center mb-2">
                                    <div class="avatar bg-danger text-white rounded-circle d-flex align-items-center justify-content-center fw-bold me-3 shadow-sm" style="width: 45px; height: 45px; font-size: 1.2rem;">
                                        @FormatUserName(review.User?.FullName, review.User?.UserName).Substring(0, 1).ToUpper()
                                    </div>
                                    <div>
                                        <div class="fw-bold text-white" style="font-size: 1rem;">@FormatUserName(review.User?.FullName, review.User?.UserName)</div>
                                        <div style="font-size: 0.8rem; color: #888;">@GetRelativeTime(review.CreatedAt)</div>
                                    </div>
                                    <div class="ms-auto text-warning fs-5">
                                        @for(int i=0; i<review.Rating; i++) { <i class="bi bi-star-fill"></i> }
                                        @for(int i=review.Rating; i<5; i++) { <i class="bi bi-star" style="color: rgba(255,255,255,0.2);"></i> }
                                    </div>
                                </div>
                                <div id="review-text-@review.Id" class="mb-0 mt-2" style="color: #ccc; font-size: 1.05rem; line-height: 1.7; padding-left: 60px;">@review.Comment</div>
                                
                                @if (User.IsInRole("Admin"))
                                {
                                    <div class="d-flex gap-2 mt-2" style="padding-left: 60px;">
                                        <button type="button" class="btn btn-sm btn-outline-light rounded-pill" onclick="document.getElementById('edit-form-@review.Id').style.display='block'; document.getElementById('review-text-@review.Id').style.display='none';"><i class="bi bi-pencil"></i> Sửa</button>
                                        <form asp-action="DeleteReview" method="post" class="d-inline" onsubmit="return confirm('Bạn có chắc chắn muốn xóa đánh giá này?');">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="reviewId" value="@review.Id" />
                                            <input type="hidden" name="destinationId" value="@Model.Id" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger rounded-pill"><i class="bi bi-trash"></i> Xóa</button>
                                        </form>
                                    </div>
                                    <form id="edit-form-@review.Id" asp-action="EditReview" method="post" style="display:none; padding-left: 60px;" class="mt-2">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="reviewId" value="@review.Id" />
                                        <input type="hidden" name="destinationId" value="@Model.Id" />
                                        <div class="d-flex flex-column gap-2">
                                            <textarea name="comment" class="form-control border-0 shadow-sm custom-dark-input" rows="2" style="border-radius: var(--radius); background: rgba(0,0,0,0.5); color: white;">@review.Comment</textarea>
                                            <div class="d-flex gap-2">
                                                <button class="btn btn-sm btn-warning rounded-pill px-3" type="submit">Lưu</button>
                                                <button class="btn btn-sm btn-secondary rounded-pill px-3" type="button" onclick="document.getElementById('edit-form-@review.Id').style.display='none'; document.getElementById('review-text-@review.Id').style.display='block';">Hủy</button>
                                            </div>
                                        </div>
                                    </form>
                                }
                            </div>
                        }
                    }
                    else
                    {
                        <div class="text-center py-5" style="color: #888;">
                            <i class="bi bi-chat-left-dots fs-1 opacity-25 mb-3 d-block"></i>
                            Chưa có đánh giá nào.<br>Hãy là người đầu tiên!
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script type="module" src="https://ajax.googleapis.com/ajax/libs/model-viewer/3.4.0/model-viewer.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const btnEnhance = document.getElementById('btnAIEnhance');
            const txtComment = document.getElementById('reviewComment');
            
            if (btnEnhance && txtComment) {
                btnEnhance.addEventListener('click', function () {
                    const keywords = txtComment.value;
                    if (!keywords.trim()) {
                        alert('Vui lòng gõ vài từ khóa (vd: đẹp, ngon) trước khi nhờ AI viết!');
                        return;
                    }
                    
                    const originalHtml = btnEnhance.innerHTML;
                    btnEnhance.innerHTML = '<span class="spinner-border spinner-border-sm me-1" role="status" aria-hidden="true"></span> Đang viết...';
                    btnEnhance.disabled = true;

                    fetch('/Destinations/EnhanceReview', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        },
                        body: JSON.stringify({ keywords: keywords })
                    })
                    .then(response => response.json())
                    .then(data => {
                        if (data.success) {
                            txtComment.value = '';
                            let i = 0;
                            const text = data.enhancedText;
                            const speed = 25; // ms per char
                            
                            function typeWriter() {
                                if (i < text.length) {
                                    txtComment.value += text.charAt(i);
                                    i++;
                                    setTimeout(typeWriter, speed);
                                } else {
                                    btnEnhance.innerHTML = originalHtml;
                                    btnEnhance.disabled = false;
                                }
                            }
                            typeWriter();
                        } else {
                            btnEnhance.innerHTML = originalHtml;
                            btnEnhance.disabled = false;
                            alert(data.enhancedText || data.message || 'Có lỗi xảy ra khi gọi AI.');
                        }
                    })
                    .catch(error => {
                        console.error('Error:', error);
                        btnEnhance.innerHTML = originalHtml;
                        btnEnhance.disabled = false;
                        alert('Có lỗi kết nối hệ thống AI.');
                    });
                });
            }
        });
    </script>
}

<style>
    .letter-spacing-1 { letter-spacing: 0.05em; }
    .custom-scrollbar::-webkit-scrollbar { width: 6px; }
    .custom-scrollbar::-webkit-scrollbar-track { background: rgba(0,0,0,0.2); border-radius: 10px; }
    .custom-scrollbar::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.2); border-radius: 10px; }
    .custom-scrollbar::-webkit-scrollbar-thumb:hover { background: rgba(255,255,255,0.4); }
    
    .cursor-pointer { cursor: pointer; }
    
    .custom-dark-input:focus {
        background-color: rgba(0,0,0,0.8) !important;
        color: #fff !important;
        box-shadow: 0 0 0 0.25rem rgba(212, 175, 55, 0.25) !important;
    }
    .custom-dark-input::placeholder {
        color: rgba(255,255,255,0.4);
    }
    
    /* Enhance the hover effect on the save button */
    .btn-outline-danger:hover {
        background-color: var(--red) !important;
        border-color: var(--red) !important;
        color: white !important;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(198, 40, 40, 0.2);
    }
    
    .btn-outline-secondary:hover {
        background: rgba(255,255,255,0.2) !important;
        color: white !important;
    }
</style>

